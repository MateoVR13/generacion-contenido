interface:
  display_name: "SCORM/PDF JSON Content"
  short_description: "3 JSON virtuales únicos y validados"
  default_prompt: "Use $aprendizaje-basado-proyectos-scorm to generate three validated <asignatura>-contenido-XX.json files (one per ABPr learning moment: Diagnóstico Inicial, Análisis y Diseño, Conclusiones) for a 100% virtual asignatura using Aprendizaje Basado en Proyectos, expert discipline-based HTML component selection, a non-repetitive 3x5 thematic diversity blueprint, six sections per file, at least three theory blocks per thematic SCORM section, bundled mandatory references, validated KaTeX-compatible LaTeX math, and separate PDF/SCORM branches without auto-loading them in the app."
