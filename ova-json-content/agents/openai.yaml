interface:
  display_name: "SCORM/PDF JSON Content"
  short_description: "7 JSON virtuales únicos y validados"
  default_prompt: "Use $ova-json-content to generate seven validated <asignatura>-contenido-XX.json files for a 100% virtual asignatura using Aprendizaje Guiado, expert discipline-based HTML component selection, a non-repetitive 7x5 thematic diversity blueprint, six sections per file, at least three theory blocks per thematic SCORM section, bundled mandatory references, validated KaTeX-compatible LaTeX math, and separate PDF/SCORM branches without auto-loading them in the app."
